# Only use LFS for model artifacts (not large data files)
artifacts/*.pkl filter=lfs diff=lfs merge=lfs -text
static/*.csv filter=lfs diff=lfs merge=lfs -text

# Exclude large data files from LFS (they're gitignored)
data/** -filter -diff -merge
